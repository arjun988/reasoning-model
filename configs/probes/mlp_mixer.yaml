# Phase 2: MLP-mixer instead of attention (~14M core on 30×30).
name: spark-7-mlp
hidden_size: 512
num_heads: 8
L_layers: 2
H_cycles: 3
L_cycles: 6
expansion: 4.0
seq_len: 900
vocab_size: 12
pos_encodings: none
mlp_t: true
use_puzzle_emb: true
puzzle_emb_len: 16
halt_max_steps: 16
forward_dtype: bfloat16
grid_size: 30
